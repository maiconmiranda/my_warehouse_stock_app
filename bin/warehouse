#!/usr/bin/env ruby

# gems
require 'tty-table'
require 'tty-prompt'
# require 'tty-markdown'

# files
require_relative '../lib/main_menu'
require_relative '../lib/product'
require_relative '../lib/product_repository'

app = MainMenu.new


  # path = File.dirname(__FILE__).split('/')
  # path.pop
  # help_database = "#{path.join('/')}my_warehouse_stock_app/public/file.md"
  # help = TTY::Markdown.parse_file(help_database)


if ARGV[0] == "-h"
  # puts help
  exit
end

if ARGV[0] == "-l" || ARGV[0] == "--list"
  app.terminal_table
else
  app.router
end